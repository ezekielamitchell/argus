# configs/ — Experiment & Model Configurations

Purpose: experiment and model configuration files (training, evaluation,
and pipeline settings).

Status: [current] empty placeholder — no configs exist yet.
Becomes active in: Phase 2 — Baseline Gaze Models.

[planned] Versioned config files for reproducible experiments. See
`docs/EVALUATION.md` for the evaluation protocol they parameterize.
