# argus/context/ — Screen / Context Feature Extraction

Purpose: extract features from screen context (screenshots) to condition
gaze and attention models on what the operator is looking at.

Status: [current] empty placeholder — no code exists yet.
Becomes active in: Phase 3 — Context-Aware Attention Models.

[planned] Screenshot feature extractors and context encoders. See
`docs/ARCHITECTURE.md` for how context fuses with gaze.
