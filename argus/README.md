# argus/ — Core Python Package

Purpose: the importable `argus` Python package housing dataset loaders,
context and gaze/attention models, the inference pipeline, and evaluation
harnesses.

Status: [current] empty placeholder — no code exists yet.
Becomes active in: Phase 2 — Baseline Gaze Models, and grows through
Phases 3–7.

[planned] Submodules: `data/`, `context/`, `models/`, `attention/`,
`pipeline/`, `evaluation/`. See `docs/ARCHITECTURE.md` for the conceptual
design.
