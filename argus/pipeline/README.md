# argus/pipeline/ — End-to-End Inference Pipeline

Purpose: wire data loading, context extraction, and models into a single
end-to-end inference path.

Status: [current] empty placeholder — no code exists yet.
Becomes active in: Phase 5 — Sensor Cueing (research), building toward
Phases 6–7.

[planned] Composition of `data/`, `context/`, `models/`, and `attention/`
into a runnable pipeline. See `docs/ARCHITECTURE.md`.
