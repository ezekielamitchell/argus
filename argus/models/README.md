# argus/models/ — Gaze & Attention Models

Purpose: model implementations for gaze estimation and, later,
context-aware attention.

Status: [current] empty placeholder — no code exists yet.
Becomes active in: Phase 2 — Baseline Gaze Models (baselines), extended in
Phase 3 — Context-Aware Attention Models.

[planned] Baseline gaze estimators and context-conditioned architectures.
See `docs/EVALUATION.md` for the evaluation protocol.
