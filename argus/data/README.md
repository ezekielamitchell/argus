# argus/data/ — Dataset Loaders & Multi-Stream Sync

Purpose: loaders and synchronization utilities for CASEset's multi-stream
records (webcam frames, screenshots, gaze coordinates, interaction logs).

Status: [current] empty placeholder — no code exists yet.
Becomes active in: Phase 2 — Baseline Gaze Models.

[planned] Time-aligned readers, stream synchronization, and train/val/test
splitting. See `docs/DATASET.md` for the CASEset schema.
